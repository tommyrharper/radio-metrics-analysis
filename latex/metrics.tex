\documentclass[11pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{hyperref}
\usepackage{parskip}
\usepackage{amsmath}
\usepackage{amssymb}

\title{Radio Interferometry Metrics}
\author{Thomas Harper}
\date{\today}

\begin{document}

\maketitle

\section*{Overview}
Summary of the key metrics involved in analysing radio interferometric

\section{Data model}

\begin{equation}
\boldsymbol{y = \Phi x^{*} + n}
\end{equation}
\begin{itemize}
    \item $\boldsymbol y\in \mathbb C^M$ is the measured visibilities
    \item $\boldsymbol\Phi: \mathbb R^N \to \mathbb C^M$  is the measurement operator
    \item $\boldsymbol{x^*}\in\mathbb R_+^N$ is the true sky image
    \item $\boldsymbol n\in\mathbb C^M$ is Gaussian noise with s.d. $\tau>0$ and mean $0$
\end{itemize}

\begin{equation}
\boldsymbol x_\text{d} = \kappa\operatorname{Re}\{\boldsymbol\Phi^\dagger \boldsymbol y\} = \boldsymbol{\mathsf Dx^* + b}
\end{equation}

\begin{itemize}
    \item $\boldsymbol x_\text{d}$ is the dirty image including noise
    \item $\kappa=\max(\operatorname{Re}{\boldsymbol\Phi^\dagger\boldsymbol\Phi\boldsymbol\delta})^{-1}>0$ is the normalisation factor ensuring the PSF has peak value 1, where $\boldsymbol{\mathbf\delta} \in \mathbb R^N$ a discrete Dirac delta centred in the image (1 at the centre, 0 elsewhere)
    \item $\boldsymbol\Phi^\dagger$ is the adjoint of the measurement operator
    \item $\boldsymbol{\mathsf D}:\mathbb R^N\to\mathbb R^N,\quad \boldsymbol{\mathsf{D}} \triangleq \kappa\operatorname{Re}\{\boldsymbol{\Phi}^{\dagger}\boldsymbol{\Phi}\}$ is a normalised map for the true image to the noise free dirty image, encoding the non-uniform Fourier sampling, gridding, de-gridding and Fourier transform operations
    \item $\boldsymbol{\mathsf D x^*}$ is the noise-free dirty image
    \item $\boldsymbol{b} = \kappa\operatorname{Re}\{\boldsymbol\Phi^\dagger\boldsymbol n\}\in\mathbf R^N$ is the normalised back projected noise
\end{itemize}

\section{R2D2}

\subsection{Internal Model}

\begin{equation}
    \boldsymbol x^{(i)} = \boldsymbol x^{(i-1)} + \boldsymbol{\mathsf N}
\end{equation}

\begin{equation}
\boldsymbol{\hat x} \triangleq \boldsymbol x^{(I)} = \sum_{i=1}^I\boldsymbol{\mathsf N}_{\boldsymbol{\hat\theta}^{(i)}}(\boldsymbol r^{(i-1)}, \boldsymbol x^{(i-1)})
\end{equation}

\begin{itemize}
    \item $\boldsymbol{\hat x}$ is R2D2s final reconstruction
\end{itemize}

\begin{equation}
(
    \boldsymbol{\mathsf N}_{\boldsymbol{\hat\theta}^{(i)}}
)
_{1\le i\le I}
\end{equation}

\begin{itemize}
    \item $\boldsymbol{\mathsf N}$ is a deep neural network (DNN)
    \item $\boldsymbol{\hat\theta}^{(i)}\in\mathbb R^Q$ are the learned parameters for a given DNN layer
    \item $I$ is the number of layers
\end{itemize}



\section{R2D2 metrics}

\subsection{Scientific Metrics}

\subsubsection{Image domain data-fidelity}

$$
\bar\sigma_{\text{res.}}(\hat{r},x_d)=\frac{\|\hat{r}\|_2}{\|x_d\|_2}
$$

\begin{itemize}
\item $\hat r$ - final dirty image residual
\item $x_d$ - true dirty image
\end{itemize}

\section{Introduction}
Introduce the problem context, motivation, research question, and contributions.
For example, prior work can be cited like this \cite{example2026}.
See Section~\ref{sec:method} for the procedure details.

\section{Theoretical Background}
Present the key theories, concepts, and prior work that support this study.
% Remove this section if not needed.

\section{Method}
\label{sec:method}
Describe design, data, materials, and analysis procedures.

\nocite{*}
\bibliographystyle{plain}
\bibliography{references}

\appendix
\section{Appendices}
Include supplementary material, extended methods, or additional results.
% Remove appendix section if not needed.

\end{document}
